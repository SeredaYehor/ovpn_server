curl -X PUT -H "Content-Type: application/json" -d '{
  "oldPassword": "{{ old_grafana_passwd }}",
  "newPassword": "{{ new_grafana_passwd }}",
  "confirmNew": "{{ new_grafana_passwd }}"
}' http://admin:admin@localhost:3000/api/user/password
